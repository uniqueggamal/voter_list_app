package com.example.voter_list_app

import io.flutter.app.FlutterApplication

class MyApplication : FlutterApplication() {

    override fun onCreate() {
        super.onCreate()

        // Initialize database
        // Example: DatabaseHelper.init(this)

        // Initialize logging
        // Example: Logger.init(this)
    }
}
